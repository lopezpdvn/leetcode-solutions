'use strict'; const log = console.log; (async ()=>{

/* Let intervals be [(startTime, endTime)] of
meetings, where startTime < endTime. Is it
possible to attend all meetings?

[(Double, Double)] -> Bool                     

const f = intervals => {
  
  // sort intervals by startTime

  // Compare endTime with starttime of adjacent
  // meeting, return False early if overlap

  // return True
};                      */

// Time complexity : O(n log(n))
// Space complexity: O(1)

// keywords: interval, overlap, intersection

})();
